# Validation Fixtures

`IDSA25OP-20240308.txt` is the bundled U.S. Department of Education 2024-25
test ISIR fixture used by the Formula A validation gate. Treat files in this
directory as deterministic input fixtures: do not write generated logs,
diagnostics, caches, or local run outputs here.

Local validation and exploration outputs belong in ignored paths such as
`.local/`, `runs/`, `outputs/`, `artifacts/`, or `logs/`.
